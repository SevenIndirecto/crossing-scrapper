[tool.poetry]
name = "border-crossing-scrapper"
version = "0.1.0"
description = ""
authors = ["sevenindirecto"]

[tool.poetry.dependencies]
python = "^3.6"
lxml = "^4.6.3"
requests = "^2.26.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
